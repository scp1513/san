package common

const (
	PortalUrl      = "https://127.0.0.1/portal/v1"
	GetIDUrl       = PortalUrl + "/server/id"
	StressUrl      = PortalUrl + "/server/stress"
	ReleaseUrl     = PortalUrl + "/server/release"
	LoginVerifyUrl = PortalUrl + "/login/verify"
)
